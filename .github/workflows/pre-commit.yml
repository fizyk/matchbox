name: Run linters

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions: {}

jobs:
  pre-commit:
    permissions:
      contents: read  # actions/checkout in the called workflow
    uses: fizyk/actions-reuse/.github/workflows/shared-pre-commit.yml@2ab3c94b032a11fca1238ef134690e589616739d # v5.7.0
